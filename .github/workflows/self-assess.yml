name: Daily Self-Assessment

on:
  schedule:
    - cron: '37 4 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
    - name: Close stale orchestration issues
      env:
        GH_TOKEN: ${{ secrets.COPILOT_ASSIGN_PAT }}
      run: |
        # Close any open orchestration issues from previous runs
        gh issue list \
          --repo "${{ github.repository }}" \
          --label "self-assess" \
          --state open \
          --json number,title \
          --jq '.[] | select(.title | startswith("Daily self-assessment")) | .number' \
        | while read -r NUM; do
          echo "Closing orchestration issue #${NUM}"
          gh issue close "$NUM" \
            --repo "${{ github.repository }}" \
            --comment "🧹 **Auto-closed by cleanup.** Copilot did not close this issue in time. Check the linked PR (if any) for Copilot's analysis summary."
        done

    - name: Close stale Copilot PRs
      env:
        GH_TOKEN: ${{ secrets.COPILOT_ASSIGN_PAT }}
      run: |
        # Close any open PRs from Copilot's self-assessment branches
        gh pr list \
          --repo "${{ github.repository }}" \
          --state open \
          --json number,headRefName \
          --jq '.[] | select(.headRefName | startswith("copilot/daily-self-assessment")) | "\(.number) \(.headRefName)"' \
        | while read -r NUM BRANCH; do
          echo "Closing PR #${NUM} (branch: ${BRANCH})"
          gh pr close "$NUM" \
            --repo "${{ github.repository }}" \
            --comment "🧹 **Auto-closed.** Self-assessment PRs are not meant to be merged — the real output is the backlog issues." \
            --delete-branch
        done

  self-assess:
    needs: cleanup
    runs-on: ubuntu-latest
    environment: copilot

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup WireGuard VPN
      run: |
        sudo apt-get update && sudo apt-get install -y wireguard resolvconf
        echo "${{ secrets.WIREGUARD_CONFIG }}" | sudo tee /etc/wireguard/wg0.conf > /dev/null
        sudo chmod 600 /etc/wireguard/wg0.conf
        sudo wg-quick up wg0

    - name: Gather metrics snapshot
      run: |
        chmod +x scripts/gather-metrics.sh
        REPORT=$(./scripts/gather-metrics.sh)
        
        # Save to file for the next step (avoids shell escaping issues)
        echo "$REPORT" > /tmp/metrics-report.md
      env:
        PROMETHEUS_URL: http://prometheus.internal:9090
        LOKI_URL: http://loki.internal
        ARGOCD_URL: http://argo.internal
        ARGOCD_AUTH_TOKEN: ${{ secrets.ARGOCD_AUTH_TOKEN }}
        ARGOCD_APP_NAME: coupon-bot
        CONTAINER_NAME: coupon-bot

    - name: Disconnect VPN
      if: always()
      run: sudo wg-quick down wg0 || true

    - name: Ensure self-assess label exists
      env:
        GH_TOKEN: ${{ secrets.COPILOT_ASSIGN_PAT }}
      run: |
        gh label create self-assess \
          --repo "${{ github.repository }}" \
          --description "Auto-discovered by daily self-assessment workflow" \
          --color "7057ff" \
          --force

    - name: Create orchestration issue
      env:
        GH_TOKEN: ${{ secrets.COPILOT_ASSIGN_PAT }}
      run: |
        DATE=$(date -u +%Y-%m-%d)
        REPORT=$(cat /tmp/metrics-report.md)

        gh issue create \
          --repo "${{ github.repository }}" \
          --title "Daily self-assessment ${DATE}" \
          --label "self-assess" \
          --assignee "@copilot" \
          --body "## Daily Self-Assessment — ${DATE}

        This is an automated self-assessment. Use the \`self-assess\` skill to analyze the metrics below, review the codebase, and manage the backlog.

        **Workflow run:** ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

        ---

        ${REPORT}

        ---

        ## Instructions

        1. Read the metrics snapshot above
        2. Analyze the codebase for quality issues
        3. Review existing open issues (especially \`self-assess\` labeled)
        4. Create new issues, bump existing ones, or close resolved ones
        5. Close this orchestration issue with a summary"
